version: '3.8'
services:
  # mongodb:
  #   image: mongo:latest
  #   container_name: civic_mongodb
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongodb_data:/data/db
  #   networks:
  #     - civic_net

  # elasticsearch:
  #   image: docker.elastic.co/elasticsearch/elasticsearch:8.11.3
  #   container_name: civic_elasticsearch
  #   environment:
  #     - discovery.type=single-node
  #     - xpack.security.enabled=false
  #     - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
  #     # CORS settings for API access if needed
  #     - http.cors.enabled=true
  #     - http.cors.allow-origin="*"
  #   ports:
  #     - "9200:9200"
  #   volumes:
  #     - es_data:/usr/share/elasticsearch/data
  #   networks:
  #     - civic_net

  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.3
    container_name: civic_kibana
    environment:
      - ELASTICSEARCH_HOSTS=https://my-elasticsearch-project-ac4a25.es.us-central1.gcp.elastic.cloud:443
      # Allow embedding in iframe (disable X-Frame-Options)
      - SERVER_SECURITYRESPONSEHEADERS_XFRAMEOPTIONS=false
    ports:
      - "5601:5601"
    networks:
      - civic_net
    # depends_on:
    #   - elasticsearch

# volumes:
  # mongodb_data:
  # es_data:

networks:
  civic_net:
    driver: bridge
